@model IEnumerable<L2_DAVH_AFPE.Models.PharmacyModel>
@using L2_DAVH_AFPE.Models.Data
@{
    ViewData["Title"] = "Index";
}

<h1>Cart</h1>

<p>
    <a asp-action="Search">Make an Order</a> -
    <a asp-action="ViewTree">View Tree</a> -
    <a class="btn btn-success" asp-action="Resuply">Resuply inventory.</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Drug Name
            </th>
            <th>
                Drug Description
            </th>
            <th>
                Price ($)
            </th>
            <th>
                Quantity
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    @Html.ActionLink("Discard", "Delete", new { item.Id })
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <th id="total" colspan="3">Total :</th>
            <td>
                @Singleton.Instance.totalre()
            </td>
        </tr>
    </tfoot>
</table>
@if (TempData["testmsg"] != null)
{
    <script type="text/javascript">
       alert("@TempData["testmsg"]");
    </script>
}
